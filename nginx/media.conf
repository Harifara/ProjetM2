server {
    listen 80;

    server_name media_service;

    location /media/ {
        alias /usr/share/nginx/html/media/;
        autoindex off;
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;
    }
}
