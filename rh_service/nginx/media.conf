server {
    listen 80;

    location /media/ {
        alias /usr/share/nginx/html/media/;  # <-- doit pointer sur le dossier monté
        autoindex off;
        add_header Access-Control-Allow-Origin *;
    }
}
